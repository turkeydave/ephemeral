{
  "name": "dispatcher",
  "version": "0.1.0",
  "private": true,
  "description": "M3 dispatcher: POST /environments mints an env_id, writes the Firestore registry doc, and creates a per-env task VM via the Compute Engine API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@google-cloud/compute": "^4.10.0",
    "@google-cloud/firestore": "^7.10.0"
  }
}
