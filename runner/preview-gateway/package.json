{
  "name": "preview-gateway",
  "version": "0.2.0",
  "private": true,
  "description": "M3 preview gateway: parses env_id from Host header, looks up the per-env task VM IP in Firestore (agent_environments/<env_id>), reverse-proxies to <vm_internal_ip>:8080. Falls back to a hardcoded VM_IP env var only when env_id == 'smoketest' (M2 compatibility).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "http-proxy": "^1.18.1"
  }
}
